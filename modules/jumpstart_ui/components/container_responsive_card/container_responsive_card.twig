<article{{ attributes.addClass('stanford-responsive-card') }}>
  <div class="responsive-card-interior-wrapper">
    <div class="responsive-card-interior">
      <div class="headline">{{ heading }}</div>
      <div class="eyebrow">{{ eyebrow }}</div>
      <div class="body">{{ body }}</div>
    </div>
    <div class="image">{{ image }}</div>
  </div>
</article>
